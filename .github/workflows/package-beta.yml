name: Package Beta

on:
  workflow_dispatch:
  push:
    tags:
      - "v*-beta.*"

permissions:
  contents: read

concurrency:
  group: package-beta-${{ github.ref }}
  cancel-in-progress: false

jobs:
  live-rest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Require live REST configuration
        env:
          LIVE_REST_CONFIG_B64: ${{ secrets.RELAY_LIVE_REST_CONFIG_B64 }}
        run: |
          if [ -z "$LIVE_REST_CONFIG_B64" ]; then
            echo "Live REST configuration is required before beta packaging."
            exit 1
          fi
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4.2.2
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Materialize protected live REST configuration
        env:
          LIVE_REST_CONFIG_B64: ${{ secrets.RELAY_LIVE_REST_CONFIG_B64 }}
        run: printf '%s' "$LIVE_REST_CONFIG_B64" | base64 --decode > "$RUNNER_TEMP/live-rest.json"
      - name: Run configured live REST acceptance suite
        env:
          RELAY_LIVE_REST_CONFIG: ${{ runner.temp }}/live-rest.json
        run: npm run test:live-rest

  package:
    needs: live-rest
    name: ${{ matrix.name }} beta package
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS Apple Silicon
            os: macos-latest
            bundles: dmg
            expected_arch: arm64
            artifact: relay-studio-macos-arm64-beta
            paths: src-tauri/target/release/bundle/dmg/*.dmg
          - name: macOS Intel
            os: macos-15-intel
            bundles: dmg
            expected_arch: x86_64
            artifact: relay-studio-macos-x86_64-beta
            paths: src-tauri/target/release/bundle/dmg/*.dmg
          - name: Windows
            os: windows-latest
            bundles: nsis,msi
            artifact: relay-studio-windows-beta
            paths: |
              src-tauri/target/release/bundle/nsis/*-setup.exe
              src-tauri/target/release/bundle/msi/*.msi
          - name: Linux
            os: ubuntu-22.04
            bundles: deb,appimage
            artifact: relay-studio-linux-beta
            paths: |
              src-tauri/target/release/bundle/deb/*.deb
              src-tauri/target/release/bundle/appimage/*.AppImage
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4.2.2
      - name: Install Linux packaging dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - run: npm ci
      - run: npm run verify
      - run: cargo test --manifest-path src-tauri/Cargo.toml
      - name: Build unsigned beta installer
        run: npm run tauri build -- --bundles ${{ matrix.bundles }}
      - name: Verify macOS package architecture
        if: runner.os == 'macOS'
        shell: bash
        run: file src-tauri/target/release/relay-studio | grep "${{ matrix.expected_arch }}"
      - name: Scan unpacked and generated bundle contents
        run: npm run check:secrets
      - name: Retain native beta installer
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.paths }}
          if-no-files-found: error
          retention-days: 14
