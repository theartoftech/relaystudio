# Sprint 18A Remediation Register

Status: Active remediation register; Sprints 18B-18D rows closed
Owner for every row: Relay Studio maintainer
Reviewed commit: `0b5b17daf274f01f63f34dbd3f07650a4eb69e50`

Severity is calibrated for a local-first, single-user developer tool. High means a realistic developer workflow can cross a hidden destination, local-file, or credential boundary. Medium includes repeatable single-process availability/integrity defects and constrained security paths. Low is defense-in-depth with no demonstrated direct compromise.

| ID | Severity | Category | Finding and impact | Exact evidence | Target | Retest condition | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RS18A-001 | High / P1 | Local file | A shared project can retain a multipart path and upload that readable file after ordinary Send without current-session reauthorization. | `src/App.tsx`; `src/services/multipartAuthorization.ts`; `src/project/projectSchema.ts` | 18C | Reopened/imported file part is unarmed; selection/approval and destination are current-session scoped; loopback receiver proves exact bytes only after approval. | Fixed/verified |
| RS18A-002 | Medium / P2 | Availability | Native response parsing and full React `<pre>` rendering can duplicate and render an arbitrarily large response until the desktop UI becomes unresponsive. | `src/services/serviceRunner.ts:263-287`; `src/App.tsx:4495-4557` | 18D | Boundary-sized response remains usable; oversized body fails or truncates with typed guidance before formatting/rendering. | Fixed/verified 18D: 5 MiB native/browser cap plus pre-format validation. |
| RS18A-003 | Low / P3 | Delivery defense | The documentation validator checks only selected OOXML relationship parts, so an external relationship can pass the gate. | `tools/validate_documentation_artifacts.py:76-106`; `:109-143` | 18E | Synthetic external relationship in any DOCX/VSDX relationship part fails with the part and target identified. | Validated/open |
| RS18A-004 | Medium / P2 | Secret handling | Secret patterns in headers, footers, comments, properties, relationships, custom XML, or embedded OOXML parts can pass documentation validation. | `tools/validate_documentation_artifacts.py:84-94`; `:128-143` | 18E | Synthetic canaries in every supported text-bearing part fail; unsupported binary parts are reported explicitly. | Validated/open |
| RS18A-005 | Low / P3 | Delivery defense | Whole-lockfile Gitleaks exclusions plus narrow custom patterns can miss credential-bearing dependency URLs or metadata. | `.gitleaks.toml:6-17`; `tools/secret-scan.mjs:12-18,54-65` | 18E | Modern token and credentialed-registry canaries in both lockfiles fail without false positives on ordinary resolved URLs. | Validated/open |
| RS18A-006 | Medium / P2 | Artifact security | The generated-artifact scan silently skips ignored extensions, files over 5 MiB, and NUL-containing binaries while reporting the candidate count as passed. | `tools/secret-scan.mjs:6-10,47-72` | 18E | Bundles are unpacked/scanned with supported/unsupported counts; a packaged canary is detected and skipped formats fail or report limitation. | Validated/open |
| RS18A-007 | High / P1 | Redaction | Saved-response metadata copies the fully resolved request URL, including userinfo or sensitive query values, into an artifact marked redacted. | `src/lib/redaction.ts`; `src/services/savedResponses.ts`; `src/project/projectSchema.ts` | 18C | URL userinfo and sensitive query canaries never survive artifact, project save, reload, comparison, diagnostics, or display. | Fixed/verified |
| RS18A-008 | High / P1 | Redaction | Raw/malformed response redaction omits common `apiKey`, `api_key`, `api-key`, and `x-api-key` spellings while marking output redacted. | `src/lib/redaction.ts`; `src/services/savedResponses.ts`; `src/services/responseComparison.ts` | 18C | Case/format canary matrix is redacted through public save/reload/compare APIs before `redacted: true` is set. | Fixed/verified |
| RS18A-009 | Medium / P2 | Availability | Saved-response comparison has no byte, nesting, node, line, or output bound before parse, recursion, splitting, and stringify amplification. | `src/services/responseComparison.ts:28-65,80-121`; `src/services/savedResponses.ts:102-119` | 18D | Exact limit succeeds; limit+1, excessive nesting/breadth, and diff amplification fail early with typed guidance. | Fixed/verified 18D: 1 MiB body, 64-level JSON, 20,000-node, 10,000-line/entry limits. |
| RS18A-010 | Medium / P2 | CI secret scope | Release Gates exposes the live-REST secret at job scope to checkout, setup, and install steps that precede the intended test. | `.github/workflows/ci.yml:77-96` | 18E | Secret exists only for materialization/test step; actions are immutable where practical; workflow contract test proves scope. | Validated/open |
| RS18A-011 | Medium / P2 | CI secret scope | Package Beta repeats the same job-wide secret exposure before mutable setup actions. | `.github/workflows/package-beta.yml:17-39` | 18E | Packaging preflight receives the secret only in the exact step and still blocks packaging when absent. | Validated/open |
| RS18A-012 | High / P1 | Network credentials | Reqwest follows a cross-origin redirect and retains arbitrary custom credential headers such as API keys. | `src-tauri/src/lib.rs` native HTTP redirect policy and loopback receiver tests | 18B | Loopback two-origin test proves all credential-class headers are removed or redirect is rejected; standard Authorization remains a negative control. | Fixed/verified 18B: cross-origin redirect is rejected before any second request. |
| RS18A-013 | High / P1 | Proxy boundary | The configured proxy bypass list reaches Rust but is never consulted, so excluded hosts can be sent through the proxy with headers/body. | `src-tauri/src/lib.rs` proxy construction, validation, and receiver tests | 18B | Exact hosts, suffixes, ports, localhost defaults, malformed entries, HTTP/HTTPS scopes, and proxy receiver behavior match documented rules. | Fixed/verified 18B: Reqwest NoProxy is applied; ports and malformed entries are explicitly rejected. |
| RS18A-014 | High / P1 | Local file | Imported saved-response metadata can name any readable `.txt` file; the native reader wraps its contents in a trusted saved-response envelope. | `src/services/savedResponsePersistence.ts`; `src-tauri/src/lib.rs` | 18C | Only app-owned/approved response artifacts reopen; disguised local text paths fail before read with recovery guidance. | Fixed/verified |
| RS18A-015 | High / P1 | Flow/network | A response mapping can overwrite `baseUrl`; later flow steps resolve credentials against the response-controlled destination. | `src/services/flowBuilder.ts` flow validation and regression test | 18B | Mapping cannot change a credentialed origin without explicit reviewed policy; controlled two-origin flow proves no secret crosses. | Fixed/verified 18B: case-insensitive `baseUrl` mappings block the flow before execution. |
| RS18A-016 | Medium / P2 | Flow correctness | Failure-condition edges do not gate target execution; only success dependencies are evaluated. | `src/services/flowBuilder.ts:319-365` | 18D | Truth-table tests cover success/failure predecessor outcomes, multiple dependencies, blocked/skipped nodes, cancellation, and visible status. | Fixed/verified 18D: success edges require success and failure edges require failure; all incoming conditions must match. |
| RS18A-017 | High / P1 | Flow/redaction | A response mapping marked non-secret can persist a credential captured from an untrusted response into exported project state. | `src/services/flowBuilder.ts`; `src/project/projectSchema.ts` | 18C | Credential canaries remain secret regardless of mapping label and never survive export, save/reload, diagnostics, or comparison. | Fixed/verified |
| RS18A-018 | Medium / P2 | Import availability | OpenAPI fetch/parse/resolution bounds reference depth and document count but not aggregate bytes, schema breadth, or expanded node/output size. | `src/services/openApiImporter.ts:155-202,219-230` | 18D | Byte, depth, document, breadth, node, and expansion limits pass at boundary and fail at boundary+1 with redacted typed errors. | Fixed/verified 18D: 2 MiB/document, 10 MiB aggregate, 20-document, 32-depth, 1,000-breadth, and 20,000-node limits. |
| RS18A-019 | Medium / P2 | Project integrity | Project validation checks top-level containers then casts untrusted nested values to `RelayProject`, allowing deterministic consumer failures. | `src/project/projectSchema.ts` | 18C | Full nested schema rejects malformed services, environments, flows, responses, imports, and settings with path-specific recovery guidance; legacy valid projects load. | Fixed/verified |
| RS18A-020 | Medium / P2 | Import destination | Swagger HTML `url`/`configUrl` discovery automatically performs a second fetch without first presenting the resolved destination for review. | `src/services/openApiImporter.ts`, `src/App.tsx`, and Playwright destination-review workflow | 18B | UI exposes and confirms the resolved secondary destination; cancel performs no second fetch; malformed/cross-origin cases are explicit. | Fixed/verified 18B: discovery and retrieval are separate explicit actions; Cancel sends no second request. |
| RS18A-021 | Medium / P2 | Import destination | A same-origin external `$ref` can redirect to another origin because validation occurs before native redirect following and final URL is not returned. | `src/services/openApiImporter.ts`, `src-tauri/src/lib.rs`, and final-origin regressions | 18B | Native response includes final identity; importer rejects a final origin mismatch and handles loops/malformed locations with typed errors. | Fixed/verified 18B: every direct/reference fetch revalidates actual final origin. |
| RS18A-022 | Medium / P2 | Availability | Native HTTP calls `response.text()` without a content-length or streamed-byte cap before crossing IPC. | `src-tauri/src/lib.rs:618-650` | 18D | Chunked and fixed-length loopback bodies enforce the same cap, cancel promptly, and never allocate/cross IPC beyond policy. | Fixed/verified 18D: 5 MiB content-length and streamed chunk checks fail before IPC body return. |
| RS18A-023 | Medium / P2 | Availability | Project open and backup restore fully read/parse files without a byte limit before schema validation. | `src-tauri/src/lib.rs:790-817` | 18D | Exact project-size boundary succeeds; larger, deeply nested, and malformed files fail before whole-file allocation with recovery guidance. | Fixed/verified 18D: 4 MiB native/browser project read/write cap covers primary and backup paths. |
| RS18A-024 | High / P1 | Project redaction | Export redacts headers and JSON bodies but leaves literal secret values in enabled query and path parameter rows. | `src/project/projectSchema.ts` | 18C | Sensitive-name and explicit-secret canaries in path/query rows are removed or converted to variables through save/reload. | Fixed/verified |
| RS18A-025 | High / P1 | Project redaction | Export leaves literal secrets in URL-encoded and multipart text form fields. | `src/project/projectSchema.ts`; `src/lib/redaction.ts` | 18C | Sensitive form-field canaries never survive project export; non-secret fields and legacy bodies remain compatible. | Fixed/verified |
| RS18A-026 | Medium / P2 | Network destination | General REST execution follows a remote redirect to localhost, LAN, or another unreviewed destination after only the original URL was visible. | `src/services/serviceRunner.ts`, `src-tauri/src/lib.rs`, and response-origin presentation | 18B | Redirect policy exposes final destination, rejects/asks according to documented policy, strips credentials, handles loops, and records redacted identity. | Fixed/verified 18B: only same-origin redirects follow; final origin is shown without userinfo/query data. |

## Dependency-Ordered Queue

| Sprint | Findings | Exit dependency |
| --- | --- | --- |
| 18B | RS18A-012, 013, 015, 020, 021, 026 | Establish the final-destination and proxy contract before persistence/flow limits depend on transport identity. |
| 18C | RS18A-001, 007, 008, 014, 017, 019, 024, 025 | Establish current-session file authority and canonical redaction before final project migration and export verification. |
| 18D | RS18A-002, 009, 016, 018, 022, 023 | Coordinate native, parser, comparison, and UI limits after transport/persistence contracts stabilize. |
| 18E | RS18A-003, 004, 005, 006, 010, 011 | Harden the delivery evidence, rerun all gates, and close the program only after every P1 row is verified. |

No finding may be closed solely because a scanner no longer reports it. Closure requires the listed failing-first regression, the smallest typed fix, targeted and representative automated suites, native interactive verification when behavior changes, redacted runtime-log inspection, and updated documentation.
